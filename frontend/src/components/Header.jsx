import React from "react";

const Header = ({}) => {

    return (
        <div className="has-text-centered m-6">
            <h1 className="title">💙 VK Tastiest 🏆</h1>
        </div>
    );
};

export default Header;